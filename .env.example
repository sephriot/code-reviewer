# GitHub Configuration
GITHUB_TOKEN=your_github_personal_access_token_here
GITHUB_USERNAME=your_github_username

# Claude Configuration
CLAUDE_PROMPT_FILE=prompts/review_prompt.txt

# Application Settings
POLL_INTERVAL=60
LOG_LEVEL=INFO

# Sound Notifications
SOUND_ENABLED=true
# SOUND_FILE=sounds/notification.wav

# Dry Run Mode (log actions instead of performing them)
DRY_RUN=false

# Database settings
DATABASE_PATH=data/reviews.db

# Optional: Specific repositories to monitor (comma-separated, format: owner/repo)
# REPOSITORIES=owner/repo1,owner/repo2